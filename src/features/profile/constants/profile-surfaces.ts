/** Profile settings surfaces — translucent cards aligned with rhythm tab. */
export const PROFILE_SURFACES = {
  settingsCard:
    "overflow-hidden rounded-[1.75rem] border border-foreground/[0.06] bg-card/35 shadow-none",
  /** Preferences editor — green border, translucent card */
  preferencesPanel:
    "overflow-hidden rounded-xl border border-primary/12 bg-card/35 shadow-none",
} as const;
